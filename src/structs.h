
typedef struct {
    // Cordinates
    int x, y;
    // Movement Variables
    int speed, direction;
}Ball;

typedef struct {
    // Cordinates
    int x, y;
    // Movement variables
    int speed;
}Player;


